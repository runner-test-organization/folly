# This file was @generated by getdeps.py

name: linux

on:
  push:
    branches:
    - build-test

jobs:
  build:
    strategy:
      max-parallel: 1
      matrix:
        runs-on: [
          # gitrunners-ubuntu-2204-x64-2vcpu, gitrunners-ubuntu-2204-arm64-2vcpu
          # ubuntu-latest,
          ubicloud-standard-2-ubuntu-2204, ubicloud-standard-2-ubuntu-2204,
          # buildjet-2vcpu-ubuntu-2204, buildjet-2vcpu-ubuntu-2204,
          # namespace-profile-x64-2x8, namespace-profile-x64-2x8,
          # warp-ubuntu-latest-x64-2x, warp-ubuntu-latest-x64-2x
        ]
    name: ${{matrix.runs-on}}
    runs-on: ${{matrix.runs-on}}
    continue-on-error: true
    steps:
    - name: cat /proc/cpuinfo
      run: cat /proc/cpuinfo

    - name: uname -mr
      run: uname -mr

    - name: nproc
      run: nproc

    - name: lscpu
      run: lscpu

    - name: free -h
      run: free -h

    - name: lsblk
      run: lsblk

    - name: df -Th
      run: df -Th
      
    - name: lspci
      continue-on-error: true
      run: lspci -vv

    - name: printenv
      run: printenv

    - name: cat /etc/environment
      run: cat /etc/environment

    - name: ls -lah
      run: ls -lah

    - name: ls -lah /home/runner
      continue-on-error: true
      run: ls -lah /home/runner
    
    - name: cat /etc/group
      run: cat /etc/group
    
    - name: groups runner
      continue-on-error: true
      run: groups runner
    
    - name: free -h
      run: free -h

    - name: sleep 500
      run: sleep 500

